{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "hero": {
      "description": "A blank schematic.",
      "factory": "./hero/index#hero"
    },
    "component": {
      "alias": [ "comp" ],
      "description": "Generate a smart or dumb component",
      "factory": "./component/index",
      "schema": "./component/schema.json"
    },
    "workspace": {
      "factory": "./workspace",
      "schema": "./workspace/schema.json",
      "description": "Create an Angular workspace for local testing.",
      "hidden": true
    },
    "application": {
      "aliases": [ "app" ],
      "factory": "./application",
      "schema": "./application/schema.json",
      "description": "Create an Angular like application for local testing."
    }
  }
}
